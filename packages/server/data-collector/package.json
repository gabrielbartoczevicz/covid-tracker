{
  "name": "@covid-tracker/data-collector",
  "version": "1.0.0",
  "description": "Application that is responsible to collect csv covid cases to parse and store it (WIP)",
  "main": "src/server.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev:server": "tsnd -r tsconfig-paths/register --inspect --transpile-only --clear --ignore-watch node_modules src/server.ts"
  },
  "dependencies": {
    "csv-parser": "^2.3.3",
    "csv-writer": "^1.6.0",
    "date-fns": "^2.16.1",
    "reflect-metadata": "^0.1.13",
    "tsyringe": "^4.3.0"
  },
  "devDependencies": {
    "@prisma/cli": "^2.7.1",
    "@types/node": "^14.6.2",
    "ts-node-dev": "^1.0.0-pre.61",
    "tsconfig-paths": "^3.9.0"
  }
}
